# Without this, commit.gpgsign in .gitconfig fails on a fresh machine because
# gpg falls back to a curses prompt that git's pager environment cannot show.
pinentry-program /opt/homebrew/bin/pinentry-mac
